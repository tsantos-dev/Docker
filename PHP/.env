# Configurações da Aplicação
APP_ENV=development # Mude para 'production' em ambiente de produção
APP_DEBUG=true      # Mude para 'false' em produção para não exibir detalhes de erro

# Configurações do Servidor Web Apache
APACHE_PORT=8081 # Ou a porta que você está usando

# Configurações do MySQL (usado pelo PHP)
MYSQL_HOST=db # Nome do serviço do contêiner MySQL no docker-compose.yml
MYSQL_PORT=3306
# Configurações do Banco de Dados MySQL (ajuste conforme necessário)
MYSQL_DATABASE=php_app_db
MYSQL_USER=php_app_user
MYSQL_PASSWORD=supersecretpassword # Troque por uma senha forte
MYSQL_ROOT_PASSWORD=rootsecretpassword # Troque por uma senha forte para o root do MySQL

# Configurações do Xdebug (opcional, descomente e ajuste se for usar)
# PHP_IDE_CONFIG=serverName=docker-php-apache
# XDEBUG_MODE=off # Mude para develop,debug para ativar
# XDEBUG_CONFIG=client_host=host.docker.internal client_port=9003 log_level=0
# Para XDEBUG_CONFIG, em Linux, pode ser necessário usar client_host=$(ip route|awk '/default/ {print $3}') se host.docker.internal não funcionar.

# Configurações do JWT (JSON Web Token)
JWT_SECRET=seuSegredoSuperSecretoAquiTroquePorAlgoLongoEAleatorio # IMPORTANTE: Troque por um segredo forte e único!
JWT_EXPIRY_SECONDS=3600 # 1 hora de validade do token
JWT_ISSUER=yourdomain.com # O emissor do token (seu domínio)
JWT_AUDIENCE=yourdomain.com # A audiência do token (seu domínio)